generated: '2026-08-13'
method: generated
source: openapi/*.yml + conventions/agile-crm-conventions.yml + errors/agile-crm-problem-types.yml + asyncapi/agile-crm-webhooks.yml
description: >-
  Packaged Agile CRM Agent Skills. Each skill covers one marquee flow and is grounded in operationIds
  that exist in this repository's OpenAPI documents — every operationId referenced below resolves to a
  real, vendor-documented endpoint. Agile CRM publishes no skills, no AGENTS.md and no llms.txt of its
  own, so these are generated by API Evangelist rather than harvested.
provider_published_skills: false
provider_published_agents_md: false
api: agile-crm:rest-api
cross_cutting_rules:
  - Base URL is tenant-scoped and templated - https://{domain}.agilecrm.com/dev
  - HTTP Basic auth - username is the account email, password is the FIRST key on Admin Settings > API & Analytics
  - Send Accept application/json on every call or the response is XML
  - All values are case-sensitive, including enums, tag names and the Basic auth username
  - No idempotency key exists - a retried create duplicates the record
  - No rate limits and no rate-limit headers are published - self-throttle
  - 204 means "no such record" on a GET and "deleted" on a DELETE
skills:
- name: agile-crm-lead-to-deal
  file: agile-crm-lead-to-deal.md
  description: Turn an inbound lead into a tracked deal with a follow-up task.
  operations:
  - getContactByEmail
  - createContact
  - addContactTags
  - addLeadScore
  - listTracks
  - createDeal
  - createTask
- name: agile-crm-contact-sync
  file: agile-crm-contact-sync.md
  description: Page the whole contact book out of the API and keep it in sync.
  operations:
  - listContacts
  - listCompanies
  - getContact
  - searchContactsAndCompanies
  - updateContactProperties
  - updateContactProperty
  - changeContactOwner
  - filterContactsDynamic
- name: agile-crm-support-ticket-triage
  file: agile-crm-support-ticket-triage.md
  description: Triage helpdesk tickets and close the loop onto the contact record.
  operations:
  - listTicketFilters
  - listTickets
  - listTicketMessages
  - addTicketNote
  - createTicket
  - deleteTicket
  - getContactByEmail
  - createNote
- name: agile-crm-campaign-enrollment
  file: agile-crm-campaign-enrollment.md
  description: Enrol and remove contacts from marketing campaigns, and what the API will not let you do.
  operations:
  - listCampaigns
  - enrollContactInCampaign
  - removeCampaignSubscriber
  - getContactByEmail
  - addTagsByEmail
